package main

// constants.go
// This file contains constants used in the application.
// Create a MySQL database named "inventory" and a table named "products" with columns "id" (UUID) and "name" (VARCHAR).
const (
	DBName     = "inventory"
	DBUser     = "root"
	DBPassword = ""
)
